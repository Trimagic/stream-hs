{
  "name": "stream-hs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "node scripts/dev.js",
    "build": "npm --prefix client run build",
    "check": "npm --prefix server run check && npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
